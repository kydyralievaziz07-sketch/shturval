# Штурвал-бэкенд почти весь на стандартной библиотеке Python.
# Исключение — раздел «Авто Рент»: приватно читает Google-таблицу аренды
# через сервисный аккаунт, для этого нужны две библиотеки Google:
google-auth
google-api-python-client
# Статический ffmpeg (без apt) — перекодируем голосовые ogg/opus → mp3 для Safari.
imageio-ffmpeg
