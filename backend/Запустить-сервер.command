#!/bin/bash
# Двойной клик по этому файлу запускает сервер-посредник «Штурвал».
# Окно нужно оставить открытым, пока пользуешься живыми данными.
cd "$(dirname "$0")"
echo "Запускаю сервер Штурвал... (не закрывай это окно)"
python3 server.py
